name: 18.3 - Reusable workflows - reusable workflow with secrets

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/18-1-reusable-workflows.yaml
    with:
      target-directory: dir

  e2e-tests:
    uses:
      sudeshnt/github-actions-reusable-workflows/.github/workflows/e2e.yaml@main
      #<owner>/<repository>/.github/workflows/<workflow-name>@<version>
      # version can be a branch, tag, or a commit SHA
    secrets:
      github-token:

  print-outputs:
    runs-on: ubuntu-latest
    needs: deploy
    steps:
      - name: Print outputs
        run: |
          echo "Build status: ${{ needs.deploy.outputs.build-status }}"
          echo "Deploy URL: ${{ needs.deploy.outputs.deploy-url }}"
